import { SelectRoomDirective } from './select-room.directive';

describe('SelectRoomDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectRoomDirective();
    expect(directive).toBeTruthy();
  });
});
